# Keep monthly run ledgers for 90 days. The files are append-only JSONL,
# so retention is handled by age-based cleanup on the parent directory
# instead of log rotation. Files older than 90 days directly under this
# directory are removed during tmpfiles cleanup.
d /var/lib/cdnfix/runs 0750 root root 90d -
